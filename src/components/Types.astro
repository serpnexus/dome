---
import { getLangFromUrl, useTranslations } from "../i18n/utils";

// Get the current language and translation function
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<section class="py-16">
  <div class="max-w-1200 mx-auto px-6">
    <h2 class="text-yellow-400 mb-4 text-center">{t("about.types.title")}</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      <div class="bg-white bg-opacity-5 border border-white border-opacity-10 rounded-lg p-6">
        <h3 class="mb-4">{t("about.types.zeroWidthSpace.title")}</h3>
        <div class="flex justify-center mb-6">
          <div class="bg-navy-700 w-16 h-16 flex items-center justify-center rounded">
            <span class="text-white text-xl">&#8203;</span>
          </div>
        </div>
        <p class="text-white">{t("about.types.zeroWidthSpace.description")}</p>
      </div>

      <div class="bg-white bg-opacity-5 border border-white border-opacity-10 rounded-lg p-6">
        <h3 class="mb-4">{t("about.types.hangulFiller.title")}</h3>
        <div class="flex justify-center mb-6">
          <div class="bg-navy-700 w-16 h-16 flex items-center justify-center rounded">
            <span class="text-white text-xl">ㅤ</span>
          </div>
        </div>
        <p class="text-white">{t("about.types.hangulFiller.description")}</p>
      </div>

      <div class="bg-white bg-opacity-5 border border-white border-opacity-10 rounded-lg p-6">
        <h3 class="mb-4">{t("about.types.zeroWidthJoiner.title")}</h3>
        <div class="flex justify-center mb-6">
          <div class="bg-navy-700 w-16 h-16 flex items-center justify-center rounded">
            <span class="text-white text-xl">&#8205;</span>
          </div>
        </div>
        <p class="text-white">{t("about.types.zeroWidthJoiner.description")}</p>
      </div>

      <div class="bg-white bg-opacity-5 border border-white border-opacity-10 rounded-lg p-6">
        <h3 class="mb-4">{t("about.types.zeroWidthNonJoiner.title")}</h3>
        <div class="flex justify-center mb-6">
          <div class="bg-navy-700 w-16 h-16 flex items-center justify-center rounded">
            <span class="text-white text-xl">&#8204;</span>
          </div>
        </div>
        <p class="text-white">{t("about.types.zeroWidthNonJoiner.description")}</p>
      </div>
    </div>
  </div>
</section>


  
